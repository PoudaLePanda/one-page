// const letters = document.querySelectorAll("#animate-logo path");

// //// get total length each path
// letters.forEach((letter, index) => {
//     console.log(index, letter.getTotalLength());
// });
